#include "main.h"

/**
 * add - adds two numbers together
 * @a: prints first number
 * @b: prints second number
 * Return: returns the result
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
